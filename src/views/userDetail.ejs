<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">


    <title>bs5 edit profile account details - Bootdey.com</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style type="text/css">
        body {
            margin-top: 20px;
            background-color: #f2f6fc;
            color: #111112;
        }

        .img-account-profile {
            height: 10rem;
        }

        .rounded-circle {
            border-radius: 50% !important;
        }

        .card {
            box-shadow: 0 0.15rem 1.75rem 0 rgb(33 40 50 / 15%);
        }

        .card .card-header {
            font-weight: 500;
        }

        .card-header:first-child {
            border-radius: 0.35rem 0.35rem 0 0;
        }

        .card-header {
            padding: 1rem 1.35rem;
            margin-bottom: 0;
            background-color: rgba(33, 40, 50, 0.03);
            border-bottom: 1px solid rgba(33, 40, 50, 0.125);
        }

        .form-control,
        .dataTable-input {
            display: block;
            width: 100%;
            padding: 0.875rem 1.125rem;
            font-size: 0.875rem;
            font-weight: 400;
            line-height: 1;
            color: #69707a;
            background-color: #fff;
            background-clip: padding-box;
            border: 1px solid #c5ccd6;
            -webkit-appearance: none;
            -moz-appearance: none;
            appearance: none;
            border-radius: 0.35rem;
            transition: border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
        }

        .nav-borders .nav-link.active {
            color: #0061f2;
            border-bottom-color: #0061f2;
        }

        .nav-borders .nav-link {
            color: #69707a;
            border-bottom-width: 0.125rem;
            border-bottom-style: solid;
            border-bottom-color: transparent;
            padding-top: 0.5rem;
            padding-bottom: 0.5rem;
            padding-left: 0;
            padding-right: 0;
            margin-left: 1rem;
            margin-right: 1rem;
            
        }
    </style>
</head>

<body>
    <div class="container-xl px-4 mt-4">

        <nav class="nav nav-borders">
            <a class="nav-link active ms-0"
                href=""
                target="__blank">Tài khoản</a>
            <a class="nav-link" href="https://www.bootdey.com/snippets/view/bs5-profile-billing-page"
                target="__blank">Đơn đặt</a>    
        </nav>
        <hr class="mt-0 mb-4">
        <div class="row">
       
        </div>
        <div class="col-xl-8 container d-flex align-items-center justify-content-center">

            <div class="card mb-4">
                <div class="card-header">Thông tin tài khoản</div>
                <div class="card-body">
                    <form style="display: inline;" action="/postUserDetail/<%= user.id %>" method="post">
                        <div class="row gx-3 mb-3">
                            <div class="mb-3">
                                <label class="font-weight-bold mb-1" for="inputUsername">Họ & tên</label>
                                <input class="form-control" id="inputUsername" name ="fullname" type="text" placeholder=""
                                    value="<%= user.fullname %>">
                            </div>
    
                            <div class="mb-3">
                                <label class="font-weight-bold mb-1" for="inputUsername">Email</label>
                                <input class="form-control" id="inputUsername" name ="email" type="email" placeholder=""
                                    value="<%= user.email %>">
                            </div>
                            <div class="mb-3">
                                <label class="font-weight-bold mb-1" for="inputEmailAddress">Số điện thoại</label>
                                <input class="form-control" id="inputEmailAddress" type="tel" name="phone"
                                    placeholder="" value="<%= user.phone %>">
                            </div>
                            <% if (user.password) { %>
                                <div class="mb-3" id="">
                                    <label class="font-weight-bold mb-1" for="inputEmailAddress">Mật khẩu</label>
                                    <input class="form-control" id="inputEmailAddress" name="password" type="password"
                                        placeholder="Cập nhật mật khẩu...">
                                </div>
                            <% } %>
                        </div>
                        <form action="/postUserDetail/<%= user.id%>" method="post">
                            <button class="btn btn-primary" type="submit">Lưu</button>
                        </form>
                      
                            
                    </form>
                    <form style="display: inline;" action="/logout" method="post">
                        <button class="btn btn-danger" type="submit">Đăng xuất</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
    </div>
    <script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js"></script>
    <script type="text/javascript">
    </script>
</body>

</html>